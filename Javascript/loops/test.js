
    let name = "Martin";
    let currentAge = 30;
    let enjoyCoding = true;

    let newAge = (currentAge + 1);
    let remainder = (newAge % 5); 
    let age = String(newAge);
    let canVote = (currentAge >= 18);

    console.log(`name: ${name}`);
    console.log(`currentAge: ${currentAge}`);
    console.log(`enjoyCoding: ${enjoyCoding}`);
    console.log(`newAge: ${newAge}`);
    console.log(`remainder: ${remainder}`);
    console.log(`age: ${age}`);
    console.log(`canVote: ${canVote}`);